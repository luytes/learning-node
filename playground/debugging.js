var person = {
  name: 'Kevin'
};

person.age = 25;

debugger; // get to specific place through debugger

person.name = 'Felix';

console.log(person);

// HOW TO DEBUG
// WRITE NODE INSPECT AND THEN THE FILE NAME

// n = next command
// c = continue through all code
// repl = explore variable values, manipulate them, mess around with js function
